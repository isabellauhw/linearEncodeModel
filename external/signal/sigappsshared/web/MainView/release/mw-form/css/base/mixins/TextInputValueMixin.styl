.mwTextField .mwInputNode, .mwTextArea .mwInputNode {
  padding-top: 1px;
  border: none;
  height: 100%;
  width: 100%;
  box-sizing: border-box;

  /* Prevents this element from shifting downward when the widget's height is reduced. Refer to g2221001 */
  display: block;
}